#include <bits/stdc++.h>

#define ALL(a) (a).begin(), (a).end()
#define RANGE_CIN(a) for (auto& x : a) std::cin >> x;
using ll = long long;

int main() {
    std::cin.tie(nullptr);
    std::ios::sync_with_stdio(false);
    return 0;
}