#include <bits/stdc++.h>

int main() {
    std::cin.tie(nullptr);
    std::ios::sync_with_stdio(false);
    int a = 0, b = 0, c = 0;
    std::cin >> a >> b >> c;
    std::cout << 21 - (a + b + c) << std::endl; 
    return 0;
}